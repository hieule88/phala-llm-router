# Pinned for reproducibility (regenerate with `pip list --format=freeze`).
# Auth service runs OUTSIDE the TEE — these are not bound to the TEE image.

annotated-types==0.7.0
anyio==4.13.0
fastapi==0.136.1
h11==0.16.0
idna==3.15
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
starlette==1.0.0
typing_extensions==4.15.0
typing-inspection==0.4.2
uvicorn==0.47.0
aiosqlite==0.21.0
slowapi==0.1.9
# Outbound HTTP client — used by nowpayments_client for invoice creation.
# httpcore 1.0.9+ supports h11 0.16 (which uvicorn also pins to).
httpx==0.28.1
httpcore==1.0.9
certifi==2025.1.31
