# Leviathan AI Edge — thin multi-tenant auth/metering proxy.
fastapi==0.136.1
uvicorn==0.47.0
httpx==0.28.1
# Ed25519 verification for wallet-bound sessions (wallet_auth.py). Falcon-512
# verification is NOT here — it lives in the wallet-verifier service, because
# Miden's Falcon variant has no Python implementation.
cryptography==48.0.1
